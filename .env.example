# Web Push VAPID Keys (generate with npm run setup)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:your-email@example.com

# Server Configuration
PORT=3000
NODE_ENV=development

# Database
DB_PATH=./database.sqlite